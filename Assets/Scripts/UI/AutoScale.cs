using UnityEngine;

//[ExecuteInEditMode]
public class AutoScale : MonoBehaviour
{
    /*private RectTransform _rect;
    private float _lenth;

    private void Awake()
    {
        _rect = GetComponent<RectTransform>();
    }
    private void Update()
    {
        if (_lenth == _rect.childCount) return;

        _rect.sizeDelta = new Vector2(0, _rect.childCount * 120);
        _lenth = _rect.childCount;
    }*/
}